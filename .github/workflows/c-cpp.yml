name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

defaults:
  run:
    working-directory: ./MP3_TAG_READ_EDIT

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      
    - name: Compile with Makefile
      run: make
      
    - name: Run the Program
      # 🛠️ Changed this line to match your compiled output name
      run: ./mp3_tag_editor
